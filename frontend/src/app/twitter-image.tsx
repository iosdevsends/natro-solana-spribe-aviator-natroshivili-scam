// Twitter card reuses the same generated image as Open Graph so twitter:image
// is also complete (summary_large_image). Kept as a re-export to avoid drift.
export { default, alt, size, contentType } from './opengraph-image';
